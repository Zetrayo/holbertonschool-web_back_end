// my comment

// Get the database name from the command line argument
var dbName = process.argv[2];

// Connect to the MongoDB database
var db = connect("mongodb://localhost:27017/" + dbName);

// Count the number of documents in the 'school' collection
var count = db.school.countDocuments();

// Output the number of documents in the 'school' collection
print("Number of documents in the 'school' collection: " + count);
