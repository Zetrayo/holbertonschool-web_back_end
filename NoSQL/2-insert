// my comment

// Define the document to insert
let doc = { name: "Holberton school" };

// Insert the document into the school collection
db.school.insertOne(doc);

// Print a success message
print("Document inserted into collection school:", JSON.stringify(doc));
